
This demonstration creates a html table for some time seires data that is stored in the file fox-wolf.csv

This requires running on a webserver to read the CSV file.


A local webserver via node can be used:

install with:
>npm install -g http-server

start server

>http-server